# nixpacks.toml
# Install ffmpeg for audio conversion

[phases.setup]
nixPkgs = ["ffmpeg", "python39"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "uvicorn api:app --host 0.0.0.0 --port $PORT"